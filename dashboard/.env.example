# Optional: full URL of the FastAPI backend (no trailing slash).
# Leave unset during `npm run dev` to use the Vite proxy (same origin, no CORS issues).
# Example direct URL: http://localhost:8000
# VITE_API_URL=http://localhost:8000
